server:
  port: 8080

#TODO: figure out why I need this here and in bootstrap.yml
spring:
  application:
    name: postalSearchApp


endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true